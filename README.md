# SamaresMBSEFramework

## Install
- ### Gradle Properties
  - Copy the file *gradle.properties.template* renaming it to *gradle.properties*.
  - Add missing properties & update existing ones if necessary to fit your environment.
  - **Make sure to never commit *gradle.properties*, and never commit any sensitive information inside *gradle.template.properties*.**

