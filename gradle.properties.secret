#Nexus artifact repository
#Server url
nexusUrl =
nexusUsername =
nexusPassword =

#Plugin Server configuration
#Plugin server url
serverUrl =
#Absolute path of descriptor.xml file on the server, to be updated upon plugin deployment
serverDescriptorPath =
#Absolute path of the folder where the plugin is to be store on the plugin server
pluginTargetUrl =

#Team Work Cloud credentials
twcUsername =
twcPassword =
ipServer =

#Maven Central (Sonatype OSSRH) credentials
mavenCentralUsername =
mavenCentralPwd =

# GPG Signing (only necessary to publish to Maven Central, is ignored for non-release versions and should only be setup
# on the CI/CD environment)
signing.gnupg.keyName =
signing.gnupg.passphrase =
signing.gnupg.executable =