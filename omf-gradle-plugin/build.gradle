apply plugin: 'groovy'
apply plugin: 'java-gradle-plugin'
apply plugin: 'maven-publish'

group 'com.samares'
version '0.115.4-SNAPSHOT'
ext.isRelease = !version.endsWith("-SNAPSHOT")

dependencies {
    testImplementation 'org.spockframework:spock-core:2.1-groovy-3.0'
}

gradlePlugin {
    plugins {
        greeting {
            id = 'com.samares.greeting'
            implementationClass = 'com.samares.gradle.plugins.GreetingPlugin'
        }
        mdPluginBuild {
            id = 'com.samares.md_plugin_build'
            implementationClass = 'com.samares.gradle.plugins.md_plugin_build.MDPluginBuild'
        }
    }
}

publishing {
    repositories {
        maven {
            allowInsecureProtocol true
            credentials {
                username nexusUsername
                password nexusPassword
            }
            def releasesRepoUrl = "$nexusUrl/maven-releases"
            def snapshotsRepoUrl = "$nexusUrl/maven-snapshots"
            url = isRelease ? releasesRepoUrl : snapshotsRepoUrl
        }
    }
}

repositories {
    mavenCentral()
}

test {
    useJUnitPlatform()
}

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(11))
    }
}